#include "encoder.h"

/*void convertToASM(char* filename, FILE* outputFile)
{
	FILE* fp =stdin
		if (filename)
		{
			fp = fopen(filename, "rb");

			if (filename == NULL)
			{
				printf("error opening file for reading");
				return -1;
			}

	    }
	char buffer[1024] = { 0 };
	int bytes_read = 0;

	while ((bytes_read = fread(buffer, sizeof(buffer), fp)) > 0)
	{
		//process data by chunks
	}

	if (fp!= stdin)
	{
		fclose(fp);
	}
	
}*/